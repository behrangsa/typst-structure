[package]
name = "typst-structure"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
typst-syntax = "0.15.0"
